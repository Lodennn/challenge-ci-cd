apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-deployment
spec:
  selector:
    matchLabels:
      app: auth-pod
  replicas: 1
  template:
    metadata:
      labels:
        app: auth-pod
    spec:
      containers:
        - name: auth-container
          image: lodenn/auth-api:1f8048e3eb979c2ddddb7112d7db354a405b0227
          env:
            - name: TOKEN_KEY
              value: secrettoken!
      restartPolicy: Always
